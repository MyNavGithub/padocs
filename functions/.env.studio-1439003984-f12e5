# Firebase Functions environment variables
# For project: studio-1439003984-f12e5
# This file is gitignored — never commit it.
#
# RESEND_API_KEY is stored in Secret Manager (set via firebase functions:secrets:set)
# Do NOT add it here — it would conflict with the secret definition.
#
# Add non-secret runtime config here if needed, e.g.:
# FRONTEND_URL=https://padocs.app
